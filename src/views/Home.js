import React from "react";
import { Button, Typography } from "@material-ui/core";

const Home = () => {
  return (
    <div>
      <Typography variant="h3">The config worked.</Typography>
      <Button>Test</Button>
    </div>
  );
};

export default Home;
